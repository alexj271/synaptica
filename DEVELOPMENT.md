
```markdown
# 🧠 Mindly — Архитектура приложения

## 🎯 Цель
Создать Telegram-бота, который:
- получает данные о сне и активности через **Health Connect**,
- анализирует их на сервере с помощью ИИ,
- предоставляет пользователю когнитивные упражнения, рекомендации и аналитику в диалоговом формате.

---

## 🏗 Общая архитектура

```

\[Health Connect] → \[Companion App] → \[API Server] ↔ \[AI Layer] ↔ \[Telegram Bot] → Пользователь

````

---

## 🔹 Компоненты системы

### 1. Telegram-бот
- Библиотека: `aiogram` (Python) или `Telegraf` (Node.js).
- Основные функции:
  - `/start` — регистрация пользователя.
  - `/morning` — утренний отчёт (сон + короткое упражнение).
  - `/focus` — напоминание о паузе и микрозадачи.
  - `/evening` — анализ дня + тренировка памяти.
  - Кнопки/инлайн-клавиатуры для выбора действий.
- Работает через **Webhook** → сервер.

---

### 2. Companion App (Android)
- Цель: забирать данные из **Health Connect** (сон, шаги, пульс, активность).
- Периодически отправляет их на сервер через REST API.
- Технологии: Kotlin + Health Connect SDK.
- Формат данных:
  ```json
  {
    "user_id": "123",
    "date": "2025-08-29",
    "sleep": 6.75,
    "steps": 7840,
    "heart_rate_avg": 72,
    "activity": "moderate"
  }
````

---

### 3. API Server

* Фреймворк: **FastAPI** (или Django REST).
* Функции:

  * Приём данных от Companion App.
  * REST API для Telegram-бота (отправка готовых сообщений).
  * Хранение профиля пользователя (PostgreSQL).
  * Планирование напоминаний (Celery/Redis).
* Эндпоинты:

  * `POST /data/health` — загрузка данных из Health Connect.
  * `GET /user/{id}/recommendations` — получить рекомендации.
  * `POST /telegram/webhook` — входящие апдейты от Telegram.

---

### 4. AI Layer

* Использует **LLM (например, GPT API)** для генерации текстов.
* Логика:

  * Анализ данных сна/активности.
  * Генерация отчётов (утро/вечер).
  * Создание когнитивных заданий (память, внимание, креативность).
  * Ведение «Сократовского диалога».
* Архитектура: отдельный модуль, вызываемый сервером по API.

---

### 5. База данных

* **PostgreSQL**
* Основные таблицы:

  * `users` — данные пользователей.
  * `health_data` — сон, шаги, пульс.
  * `sessions` — сессии в Telegram.
  * `recommendations` — сохранённые советы и упражнения.
  * `usage_logs` — статистика взаимодействия.

---

## 🔹 Потоки данных

1. **Регистрация**
   Пользователь пишет в Telegram → бот регистрирует → создаётся профиль в БД.

2. **Утро**
   Companion App шлёт данные сна → сервер сохраняет → AI Layer генерирует отчёт → бот отправляет пользователю.

3. **День**
   Сервер (через планировщик) вызывает AI Layer → бот шлёт напоминания и упражнения.

4. **Вечер**
   Данные активности за день → сервер → AI Layer → вечерний отчёт + тренировка памяти → бот пользователю.

---

## 🔹 MVP-функционал (первая версия)

1. Telegram-бот с 3 сценариями:

   * Утренний отчёт.
   * Дневные микро-практики.
   * Вечерний анализ.
2. Companion App: сбор сна, шагов, активности.
3. Сервер с API + БД.
4. AI Layer: простая генерация текста через GPT API.
5. Простая аналитика (сон, шаги, фокус).

---

## 🚀 Дальнейшее развитие

* Добавить **геймификацию** (очки, уровни, награды).
* Интеграция с календарём (Google/Outlook).
* Поддержка iOS (через Apple Health).
* Расширенные когнитивные программы (30-дневные курсы).
* B2B-режим: дашборды для компаний (анонимные данные сотрудников).
